# CLAUDE.md

This file is a compatibility entrypoint for Claude Code (`claude.ai/code`).

The authoritative repository manual is [`AGENTS.md`](../AGENTS.md). It contains the file map, build/test commands, architecture overview, source-of-truth rules, common pitfalls, and `Do not do this` guardrails for the `src/` tree.

Human-facing install, run, build, deploy, and verification commands live in [`README.md`](../README.md), including the `gh run list` deploy check.

For which product specs and playtest reports are current versus stale, see [`docs/AGENT-READING-LIST.md`](../docs/AGENT-READING-LIST.md).
