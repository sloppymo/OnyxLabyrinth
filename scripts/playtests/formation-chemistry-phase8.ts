/**
 * Formation Chemistry Phase 8 lab — superseded.
 *
 * The fixed-duo-party campaign always uses the Old Man and the Rat King,
 * so the four-preset formation matrix no longer has meaning. This file is
 * kept as a placeholder to preserve the evidence trail; the next balance
 * pass for the duo should write a new lab script against `createDefaultParty()`.
 *
 * Running this script is a no-op.
 */

console.log("formation-chemistry-phase8 is obsolete in the fixed-duo-party branch.");
console.log("Use a fresh lab script driven by createDefaultParty() instead.");
